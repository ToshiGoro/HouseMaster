server:
  port: 8080

spring:
  application:
    name: stores

  datasource:
    url: jdbc:postgresql://localhost:5432/house_db
    username: postgres
#    password: ${DATABASE_PASSWORD:}
    password: SufferingIsAWayToHeaven

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
      format_sql: true

  web:
    resources:
      add-mappings: false

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    path: /swagger-ui.html



